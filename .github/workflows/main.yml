name: 刷课

on:
  # 关闭push触发、关闭定时，只保留手动触发
  workflow_dispatch:

jobs: 
  Start:
    runs-on: ubuntu-latest
    steps:
      - name: 拷贝代码
        uses: actions/checkout@v3

      - name: 设置python环境
        uses: actions/setup-python@v2
        with:
          python-version: '3.11'

      - name: 安装依赖包
        run: |
          pip install -r ./requirements.txt 
          
      - name: Run main.py
        run: python main.py -u 17317387959 -p SSY0910yj@ -l 262220431
